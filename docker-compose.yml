services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: uec_csds
      POSTGRES_USER: uec
      POSTGRES_PASSWORD: uec
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
